# Hexagon Dev UI

Hexagon AI Agent 框架的开发调试界面，用于实时监控和调试 Agent 执行过程。

## 功能特性

- 🔄 **实时事件流** - 通过 SSE 实时接收 Agent 执行事件，支持暂停/恢复
- 📊 **指标仪表板** - 实时展示运行指标（事件数、Agent 运行数、LLM 调用数等）
- 🔍 **事件详情** - 查看事件的完整 JSON 数据，支持 trace 链路追踪
- 🔧 **事件过滤** - 按类型过滤事件（Agent、LLM、Tool、Retriever、Graph、Error）
- 💬 **流式输出** - LLM 流式输出按 run_id 聚合实时显示
- 🌙 **暗色主题** - GitHub 风格的暗色界面，适合长时间调试

## 界面布局

```
┌─────────────────────────────────────────────────────────────────┐
│  Hexagon DevUI                          ● Connected    [暂停]   │
├───────────────┬─────────────────────────────┬───────────────────┤
│               │                             │    📊 实时指标     │
│   事件列表     │       事件详情              │   ─────────────   │
│   (可过滤)     │       (JSON 视图)           │   总事件数: 156   │
│               │                             │   Agent 运行: 3   │
│   agent_start │                             │   LLM 调用: 12    │
│   llm_request │                             │   Tool 调用: 8    │
│   tool_call   │                             │   错误数: 0       │
│   ...         │                             │   运行时间: 05:23 │
│               │                             │                   │
├───────────────┴─────────────────────────────┴───────────────────┤
│  Hexagon DevUI v0.1.0                                           │
└─────────────────────────────────────────────────────────────────┘
```

## 技术栈

- **框架**: Vue 3.5 + Composition API
- **语言**: TypeScript 5.5 (严格模式)
- **构建**: Vite 5.4
- **通信**: Server-Sent Events (SSE)

## 开发

```bash
# 安装依赖
npm install

# 启动开发服务器 (需要先启动 hexagon devui 后端)
npm run dev

# 构建生产版本
npm run build

# 预览构建产物
npm run preview

# 类型检查
npm run typecheck

# 复制构建产物到 hexagon
npm run copy
```

## 事件类型

| 类型 | 事件 | 说明 |
|------|------|------|
| **Agent** | `agent_start`, `agent_end` | Agent 生命周期事件 |
| **LLM** | `llm_request`, `llm_stream`, `llm_response` | LLM 调用和流式输出 |
| **Tool** | `tool_call`, `tool_result` | 工具调用和返回结果 |
| **Retriever** | `retriever_start`, `retriever_end` | 检索器执行事件 |
| **Graph** | `graph_start`, `graph_node`, `graph_end` | 图执行流程事件 |
| **State** | `state_change` | 状态变更事件 |
| **Error** | `error` | 错误事件 |

## API 端点

前端通过以下端点与后端通信（开发模式下 Vite 代理到 `localhost:8080`）：

| 端点 | 方法 | 说明 |
|------|------|------|
| `/events` | SSE | 事件流端点，推送实时事件 |
| `/api/metrics` | GET | 获取运行指标（每秒轮询） |

## 目录结构

```
hexagon-ui/
├── src/
│   ├── App.vue              # 根组件（三栏布局 UI）
│   ├── main.ts              # 入口文件
│   ├── composables/
│   │   ├── useSSE.ts        # SSE 连接管理（自动重连、事件缓冲）
│   │   └── useMetrics.ts    # 指标轮询和格式化
│   ├── types/
│   │   └── event.ts         # 事件和指标的 TypeScript 类型定义
│   └── styles/
│       └── main.css         # 全局样式（暗色主题）
├── vite.config.ts           # Vite 配置（代理、构建优化）
└── tsconfig.json            # TypeScript 配置
```

## 与 Hexagon 集成

### 开发模式

1. 启动 Hexagon DevUI 后端：
   ```bash
   cd /path/to/hexagon
   go run examples/devui/main.go
   ```

2. 启动前端开发服务器：
   ```bash
   cd /path/to/hexagon-ui
   npm run dev
   ```

3. 访问 http://localhost:5173

### 生产模式

1. 构建前端：
   ```bash
   npm run build
   npm run copy  # 复制到 hexagon/observe/devui/static/
   ```

2. 重新构建 Hexagon，静态文件会被嵌入到二进制文件中。

## 配置

### Vite 代理配置

开发模式下，Vite 将 API 请求代理到后端：

```ts
// vite.config.ts
proxy: {
  '/api': 'http://localhost:8080',
  '/events': 'http://localhost:8080'
}
```

如需连接其他后端地址，修改 `vite.config.ts` 中的代理目标。

### 事件缓冲

- 事件列表最多保留 **1000** 条记录
- SSE 断开后 **3 秒**自动重连
- 指标数据每 **1 秒**刷新一次

## 许可证

MIT
